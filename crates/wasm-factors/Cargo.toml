[package]
name = "wasm-factors"
version = "0.1.0"
edition = "2021"
description = "Alpha158 factor library — 158 A-share quantitative factors (Qlib-compatible)"

[dependencies]
wasm-core = { path = "../wasm-core" }
serde = { version = "1", features = ["derive"] }
